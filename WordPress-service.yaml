apiVersion: v1
kind: Service
metadata:
  name: mc-service
  annotations:
     cloud.google.com/backend-config: '{"ports": {"80":"backendconfig-example"}}'
spec:
  selector:
    app: wordpress
    tier: frontend
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
